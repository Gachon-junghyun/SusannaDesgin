@AGENTS.md

## 이 저장소에서 작업할 때

**구조 지도: [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)**
루트 → 페이지(P1~) → 기능(F1~) 트리, 설계 원칙 A1~A6, 미완료 부채가 들어 있습니다.

1. **코드를 고치기 전에** 해당 영역의 절을 읽습니다.
2. **고친 뒤에는** 문서를 갱신하고 **적합성 판단 결과를 보고**합니다.

두 절차의 상세 항목과 보고 형식은 위 `AGENTS.md` 의
&ldquo;구조 지도를 항상 최신으로 유지할 것&rdquo; 절에 있습니다.

## 마케팅 운영 상태는 형제 저장소에 있습니다

이 저장소는 **홈페이지 코드**입니다. 그런데 이 회사 마케팅의 상당 부분은 코드 밖에서
돕니다 — 네이버 스마트플레이스·예약, 구글 비즈니스 프로필, 당근, 숨고, 블로그, 리뷰.
**지금 무엇이 검수 중이고 무엇이 밀렸는지**는 여기가 아니라 형제 저장소
**`DeGaJa_Agent`** 의 `hanjeonghyun / susanna` 도메인에 있습니다.

**읽는 법** — MCP 서버 `degaja-domains` 가 붙어 있습니다.

```
mcp__degaja-domains__read_domain  { account: "hanjeonghyun", domain: "susanna" }
```

`DOMAIN.md`(운영 계약)와 `notes.md`(대기 항목 원장)를 **원문 그대로** 돌려줍니다.
도구가 안 보이면 `.mcp.json` 이 없는 것입니다 — 이 PC 의 절대경로가 들어가는 파일이라
**git 에 올리지 않습니다**(저장소가 공개입니다).

⚠️ **`PLAYBOOK.md`(채널별 실행 규칙·금지)는 파일 이름만 넘어오고 본문은 안 옵니다.**
블로그 글·플레이스 새소식·광고 문구 초안을 여기서 쓰게 되면, 규칙을 기억으로 지어내지 말고
**형제 저장소에서 그 파일을 직접 읽어야 합니다.** 잘못 쓰면 프로필 정지까지 갑니다.

**언제 읽나**

- `docs/SEO.md` 의 **C 섹션**(사람이 해야 하는 일)이나 `§7 미완료·부채`를 손대기 전에
- 검색 노출·채널 등록·견적 알림·플레이스 관련 요청을 받았을 때
- "이거 이미 했나?" 싶을 때 — **채널 상태의 최신값은 그쪽 `notes.md`** 입니다

**읽기 전용입니다.** 쓰기 도구가 없습니다(그쪽 `DOMAIN.md` 는 무인 실행의 지시문이라
아무 세션이나 고치지 못하게 막아 두었습니다). 상태가 바뀌었으면 그쪽 저장소에서 고칩니다.

| 무엇 | 원본 |
|---|---|
| 코드 구조·기능·부채 | 이 저장소 `docs/ARCHITECTURE.md` |
| 검색 노출 진단·채널 등록 절차 | 이 저장소 `docs/SEO.md` |
| 회사 정보·SEO 문구 | 이 저장소 `config/site.ts` |
| **채널별 현재 상태·대기 항목·발행 이력** | **형제 저장소 `susanna` 도메인의 `notes.md`** |
| **채널 운영 주기·판단 기준·금지 목록** | **형제 저장소 `susanna` 도메인의 `PLAYBOOK.md`** |

같은 사실을 양쪽에 적지 마세요. 어긋나는 순간 둘 다 못 믿게 됩니다.
