# 검색 상위노출 — 가이드 적용 현황

> 출처: [`대전_간판업체_네이버구글_상위노출_가이드.md`](../대전_간판업체_네이버구글_상위노출_가이드.md)
> 이 문서는 그 가이드를 **이 코드베이스 기준으로 실행 가능하게 쪼갠 것**입니다.
> 최종 갱신: 2026-07-28

## 현재 색인 상태 — 실측 (2026-07-27, 구글 한국 `hl=ko&gl=kr`)

> ⚠️ **미국 기준 검색 도구로는 이 결과가 안 보입니다.** 한 번 그것만 보고
> "인덱스 0건" 이라고 잘못 판정한 적이 있습니다. 반드시 `hl=ko&gl=kr` 로 확인하세요.

| 검사 | 결과 |
|---|---|
| `https://susannadesign.co.kr/` | ✅ 200 · 112KB · 1.5s |
| Googlebot·GPTBot·OAI-SearchBot·ChatGPT-User·PerplexityBot·ClaudeBot·Yeti·bingbot | ✅ **8종 전부 200** — 차단 0건 |
| `robots.txt` · `<meta robots>` · canonical · title · description · JSON-LD · sitemap | ✅ 전부 정상 |
| **"수산나디자인" 검색** | ✅ **자사 홈페이지가 1위** — 우리가 넣은 타이틀·설명 그대로 |
| **`site:susannadesign.co.kr`** | ⚠️ **홈 1페이지뿐** — 나머지 9개 주소 미색인 |
| **AI 개요(제미나이)가 인용하는 출처** | 🔴 잡코리아 · RnDcircle · 인스타그램 · 비즈노 — **자사 홈페이지 없음** |

기술적으로 막힌 곳은 없습니다. 남은 문제는 두 가지입니다.

### ⚠️ 문제 1 — 홈 말고는 색인이 안 됐습니다

> **🔴 2026-08-06 Search Console 실측으로 원인이 뒤집혔습니다. 아래 옛 진단은 틀렸습니다.**
>
> **사이트맵은 이미 제출돼 있었습니다** — `제출 2026.7.27 · 마지막 읽음 2026.8.3 · 상태 성공 · 발견된 페이지 7`.
> 그리고 미색인 사유는 "발견 안 됨"이 아니라 **`크롤링됨 - 현재 색인이 생성되지 않음`** 입니다.
> 크롤링 허용 `예`, 페이지 가져오기 `성공`, 사이트맵에서 발견까지 전부 정상이었습니다.
>
> **즉 구글이 다 읽어 놓고 색인할 가치가 없다고 판단한 것입니다.** 기술적으로 막힌 게 없습니다.
>
> `/works` `/about` `/process` `/support` `/quote` 5건에 **색인 생성 요청을 넣었지만**
> (`/signs` 는 이미 색인됨), 구글이 화면에 직접 **"페이지를 여러 번 제출해도 대기열 위치나
> 우선순위가 변경되지 않습니다"** 라고 적어 둡니다. **재요청은 해법이 아닙니다.**
>
> `크롤링됨 - 현재 색인 생성 안 됨` 의 통상 원인은 **얇은 콘텐츠 + 신규 도메인**입니다.
> 우리 `/works` 는 사진 카드 23개에 제목뿐이고 본문이 거의 없습니다.
> **→ 진짜 해법은 B-1 시공사례 개별 페이지입니다.** 이번 진단이 그걸 데이터로 확인해 줬습니다.

### 🔴 문제 2 — AI 가 자사 홈페이지 대신 채용·기업정보 사이트를 인용합니다

제미나이·구글 AI 개요가 회사를 **알고는 있는데**, 근거로 잡코리아·RnDcircle·
비즈노·인스타그램을 답니다.

**차단 문제가 아닙니다.** 크롤러 8종이 전부 200 으로 통과하는 것을 실측했습니다.
**AI 인용은 "차단을 푸는 문제"가 아니라 "인용할 사실을 주는 문제"입니다.**

| 남은 원인 | 무엇이 필요한가 |
|---|---|
| **색인된 지 며칠 안 됨** | AI 개요의 근거 자료는 색인보다 **몇 주** 늦게 갱신됩니다 — 기다리는 것 외엔 없습니다 |
| **디렉터리가 더 많은 사실을 갖고 있음** | 설립연도·대표자·사업자번호 같은 정형 사실은 이제 우리도 구조화 데이터로 냅니다(`taxID`·`sameAs`·`WebSite`). 남은 건 **사진 50자리** 와 **시공사례 상세** 입니다 |

> 엔티티 신호(`sameAs`·`taxID`·`WebSite`)는 AI 가 "이 홈페이지 = 그 회사" 로
> **식별**하게 합니다. 하지만 인용할 **내용**을 만드는 건 사진과 시공사례입니다.

### 네이버 URL 검사 느낌표 — 처리 결과 (2026-07-28)

소유확인 직후 URL 검사에서 느낌표 3건이 떴고, 파는 과정에서 2건을 더 찾았습니다.

| # | 항목 | 진단 | 처리 |
|---|---|---|---|
| 1 | 페이지 설명 80자 초과 | 전 페이지 89~107자 | ✅ **43~62자**로 재작성 |
| 2 | OG 설명 80자 초과 | 96자 | ✅ 위와 같은 값으로 통일 |
| 3 | `robots.txt` 없음 | ❌ **오탐** — Yeti UA 로 실측 시 `200 / text/plain / 500B`, 내용 정상, Yeti 차단 없음 | 코드 문제 아님. **배포 후 URL 검사 재실행**해서 그때도 뜨면 다시 팝니다 |
| 4 | **OG 설명이 전 페이지 동일** | 각 `page.tsx` 가 `description` 만 적고 `og:description` 은 아무도 안 적어 **10개 페이지가 전부 홈 설명을 공유**. 어느 페이지를 카톡에 붙여도 홈 소개가 떴습니다 | ✅ `lib/seo.ts` `pageMetadata()` 로 통합 |
| 5 | **사이트맵에 `noindex` 3개** | `/privacy` `/terms` `/no-email-collect` 를 제출하면서 동시에 `noindex` → **모순 신호**. 제출 10건 중 3건이 영구 실패로 남습니다 | ✅ 사이트맵 **7건**으로 축소 |

4·5번은 운영 사이트에서 실제로 재현해 확인한 뒤 고쳤습니다.

> ⚠️ **4번을 고칠 때의 함정** — Next.js 는 메타데이터를 **얕게 병합**합니다.
> 페이지에서 `openGraph` 를 **한 필드라도** 적으면 `layout.tsx` 의 `openGraph` 가
> 통째로 교체돼 **`og:image` 가 조용히 사라집니다.** 화면엔 아무 표시도 안 납니다.
> 그래서 `pageMetadata()` 가 매번 `openGraph` 전체를 다시 채웁니다 —
> 페이지에서 직접 `openGraph` 를 적지 마세요. (근거: `node_modules` 의
> `generate-metadata.md` §Merging)

## 요약: 홈페이지로 할 수 있는 일은 전체의 절반뿐입니다

가이드의 핵심은 네이버와 구글의 메커니즘이 다르다는 것입니다.

| | 홈페이지 기여도 | 나머지 |
|---|---|---|
| **구글** | 8할 — 코드 품질·구조화 데이터가 직접 순위 | GBP 2할 |
| **네이버** | 2할 — 서치어드바이저 색인 정도 | 플레이스·블로그 8할 |

즉 **코드를 아무리 잘 짜도 네이버 노출은 거의 안 움직입니다.** 네이버는 플레이스 등록과
블로그 운영이 실체이고, 그건 사람이 해야 합니다. 아래 C 섹션이 그 목록입니다.

---

## A. 코드에 적용 완료 (2026-07-24)

### A-1. 온페이지 — 지역 키워드 배치

가장 큰 구멍이었습니다. 기존 타이틀이 `회사소개 | 수산나디자인` 형태라
**"대전 간판"으로 검색하면 잡힐 근거가 아예 없었습니다.**

| 페이지 | 이전 | 이후 | 자수 |
|---|---|---|---|
| `/` | 수산나디자인 \| 옥내외광고물 · 간판디자인 · 철구조물 전문업체 | **대전 간판제작·옥외광고물 전문** \| 수산나디자인 | 25 |
| `/about` | 회사소개 | **대전 간판업체** 회사소개 | 21 |
| `/signs` | 사업영역 | **대전 간판·옥외광고물** 사업영역 | 25 |
| `/works` | 주요실적 | **대전 간판 시공사례** | 19 |
| `/process` | 업무프로세스 | **간판 제작·시공** 업무프로세스 | 24 |
| `/quote` | 무료 견적 문의 | **대전 간판** 무료 견적 문의 | 23 |
| `/support` | 고객지원 | 고객지원 · 자주 묻는 질문 | 24 |

전부 60자 이내(가이드 기준)이며 키워드가 앞쪽에 있습니다.

**메타 설명**: 홈이 361자라 검색결과에서 잘리고 있었습니다 → 재작성.

> ⚠️ **80자가 상한입니다.** 처음엔 "한글은 90~110자" 로 잡아 전 페이지를 89~107자로
> 맞췄는데, **네이버 서치어드바이저 URL 검사가 느낌표를 냈습니다**(2026-07-28, 96자).
> 가이드의 "150~160자" 는 영문 기준이고 한글 한계선은 그보다 훨씬 낮습니다.
> → 전 페이지 **43~62자**로 다시 줄였습니다. 값은 `config/site.ts` 의
> `seo.homeDescription` 과 `seo.pages` 에 모여 있습니다 [A5].

**키워드**: `config/site.ts` 의 `seo.primary` 로 이전. 지역 × 서비스 × 의도 3레이어 중
**실제로 하는 일만** 골랐습니다. 하지 않는 서비스를 키워드로 넣으면 들어온 사람이
바로 이탈해 오히려 순위에 해롭습니다.

### A-2. 구조화 데이터

| 스키마 | 위치 | 상태 |
|---|---|---|
| `LocalBusiness` | 전 페이지 (`app/layout.tsx`) | 대폭 보강 |
| `FAQPage` | `/support` | **신규** — 질문 8개 |
| `BreadcrumbList` | 서브 페이지 9개 | **신규** |

**고친 버그**: 기존 `openingHours: "Mo-Su 08:00-19:00"` 이 실제 정보
(`평일 09:00~18:00 / 주말·공휴일 휴무`)와 **어긋나 있었습니다.**
가이드가 반복해 경고하는 NAP 불일치이고, AI 요약이 틀린 영업시간을 인용하게 됩니다.
→ `openingHoursSpecification` 으로 교체해 `config/site.ts` 와 일치시켰습니다.

**추가한 것**: `geo`(좌표 — 로컬팩의 "거리" 요인), `areaServed`(출장 시공 지역 9곳),
`image` · `logo` · `founder` · `foundingDate` · `sameAs` · `addressRegion`.

**일부러 넣지 않은 것**: `AggregateRating` · `Review`.
자사 사이트에 실제 게시된 리뷰에만 쓸 수 있고, 플레이스·GBP 리뷰를 긁어 넣으면
**리치 결과 제외 또는 수동 조치** 대상입니다 (가이드 §3.3, §6).

### A-3. 네이버 서치어드바이저 대응

| 항목 | 상태 |
|---|---|
| `sitemap.xml` | **7건** — `/admin` 과 **`noindex` 3개**(`/privacy` `/terms` `/no-email-collect`) 제외. 목록의 단일 출처는 `config/site.ts` 의 `noindexPaths` |
| **`rss.xml`** | **신규** — 서치어드바이저는 사이트맵과 **RSS 를 함께** 받습니다. 시공사례 15건 자동 반영 |
| 소유확인 메타태그 | ✅ **네이버 값 입력 완료** (2026-07-28). `googleVerification` 은 DNS 방식이라 공란 유지 |
| Yeti(네이버 봇) 차단 여부 | 차단 없음 확인 |
| `robots.txt` | `/admin` 차단 추가 (이전에 누락) |

### A-4. 기술적 SEO (이미 충족)

| 항목 | 상태 |
|---|---|
| WebP 변환 · lazy loading | `next/image` 기본 제공 |
| 모바일/PC 동일 콘텐츠 | 반응형 단일 마크업 |
| `tel:` 클릭 링크 | `site.phoneHref` |
| canonical | 전 페이지 지정 |
| H1 1개 원칙 | 홈 1개(히어로), 서브 1개(`PageHero`) — 검증 완료 |
| URL 하이픈 구조 | `/no-email-collect` 등 |
| HTTPS | 배포 환경 사안 |

---

## B. 코드로 가능하지만 **콘텐츠가 먼저** 필요한 것

### B-1. 시공사례 개별 페이지 ★ 가장 효과가 큼

가이드 §3.6: *"시공사례 페이지는 로컬 SEO에서 가장 강력한 콘텐츠 자산."*
각 사례를 별도 URL로 발행하면 `대전 유성구 카페 채널간판 시공` 같은 롱테일을 자동으로 덮습니다.

**아직 만들지 않았습니다.** 지금 데이터로 상세 페이지를 만들면 제목·업종·사진 한 장뿐이라
가이드 §3.7이 경고한 **"얇은 콘텐츠"** 가 되어 오히려 페널티 대상이 됩니다.
2026년 상반기 코어 업데이트에서 이 패턴이 강하게 처벌받았습니다.

필요한 것 — 사례당:
- 시공 위치(구/동 단위) · 간판 종류 · 시공 기간 · 사용 자재 · 시공 전후 사진
- 현장에서 겪은 구체적 내용 3~4문장 (벽면 재질, 고소작업 여부 등)

이 필드를 DB에 추가하고 관리자 화면에서 채울 수 있게 하는 건 준비되면 바로 가능합니다.
**최소 3~5건이라도 제대로 채운 뒤 발행**하는 편이 15건을 얇게 까는 것보다 낫습니다.

### B-2. 지역별 페이지
`/대전-서구-간판` 식 페이지는 **지명만 바꾼 템플릿이면 확실히 페널티**입니다(§3.7).
해당 구의 실제 시공사례가 3건 이상 쌓인 뒤에만 만듭니다. 지금은 하지 않는 게 맞습니다.

### B-3. 정보성 콘텐츠
`옥외광고물 허가 절차`, `대전 강풍 대비 간판 시공 시 주의점` 같은 글.
E-E-A-T의 "경험" 축을 채우는 항목이라 업계 일반론 복사는 역효과입니다.

---

## C. 대표님이 직접 하셔야 하는 것 (코드로 불가)

**네이버 노출의 8할이 여기 있습니다.** 순서대로 하시면 됩니다.

### 1주차 — 등록 (이번 주에 다 끝낼 수 있음)

| # | 할 일 | 소요 | 주소 |
|---|---|---|---|
| C1 | ✅ **네이버 스마트플레이스 등록 완료** (2026-08-06) — 사진·소개글·가격정보 9건·영업시간·부가정보까지 채웠습니다. 남은 건 대표키워드·전화번호(기본정보 검토 대기)와 새소식 (C1 상세) | 심사 2시간~5일 | smartplace.naver.com |
| C1-b | ⏳ **네이버 예약 검수 진행중** (2026-08-06 신청) — 상품 `간판 무료 상담` 노출중. 통과 후 `예약받기 시작` + `플레이스 연결하기` 두 클릭이면 플레이스에 [예약] 버튼이 붙습니다 (C1-b 상세) | 영업일 2~3일 | partner.booking.naver.com |
| C2 | ⏳ **구글 비즈니스 프로필 — 소유권 주장 진행중** (2026-08-06). 🔴 **남이 만든 미소유 등록이 이미 있었고 내용이 틀렸습니다**(`그래픽 디자이너 / 유성구 원내동 98-1`). 카테고리·주소·전화를 교정했고 **인증만 남았습니다** (C2 상세) | 인증 방식은 구글이 지정 | business.google.com |
| C3 | ✅ **소유확인 완료** (2026-07-28, HTML 태그) — 사이트맵·RSS 제출이 남았습니다 | 색인 14~16일 | searchadvisor.naver.com |
| C4 | ✅ **소유확인 완료** (2026-07-28, 도메인 DNS TXT — 실측 확인) — 사이트맵 제출·색인 요청이 남았습니다 | 즉시 | search.google.com/search-console |
| C5 | ✅ **당근 비즈프로필 등록 완료** (2026-07-28) — 이름 `수산나디자인 간판제작`. 운영·광고는 C5 상세 | 즉시 | business.daangn.com |
| C6 | **숨고(고수) 등록** — 간판 제작 카테고리 존재. 견적 발송이 유료라 C6 상세를 먼저 읽으세요 | 등록 즉시 | soomgo.com/pro |
| C7 | 카카오맵 등록 | | kakao.com/business |

> C3·C4의 소유확인 코드는 저에게 주시면 `config/site.ts` 에 넣어 드립니다.
> (또는 직접 `naverVerification` / `googleVerification` 에 붙여넣으시면 됩니다.)

---

### C4 상세 — Google Search Console (★ 이거 하나면 구글 문제가 풀립니다)

**5분입니다.** 지금 구글에 사이트가 안 나오는 유일한 이유가 이 단계를 안 해서입니다.

**1) 등록** — [search.google.com/search-console](https://search.google.com/search-console)
접속 → 구글 계정 로그인 → 왼쪽 위 **속성 추가**

**2) 방식 선택 — 왼쪽 "도메인"을 고르세요**

| | 소유확인 방법 | 재배포 | 권장 |
|---|---|---|---|
| **도메인** | Cloudflare DNS 에 **TXT 레코드** 1줄 추가 | **불필요** | ★ 이쪽 |
| URL 접두어 | HTML 태그 코드를 코드에 넣고 배포 | 필요 | 차선 |

`susannadesign.co.kr` 을 입력하면 구글이 `google-site-verification=...` 로 시작하는
TXT 값을 줍니다. 도메인을 이미 Cloudflare 에서 관리하시니 붙여넣기로 끝납니다.

**3) TXT 넣기** — [dash.cloudflare.com](https://dash.cloudflare.com) → `susannadesign.co.kr`
→ **DNS** → **Record 추가**

| 항목 | 값 |
|---|---|
| Type | `TXT` |
| Name | `@` (도메인 루트 그대로) |
| Content | 구글이 준 `google-site-verification=...` 전체 |
| Proxy | TXT 는 프록시 설정이 없습니다 |

저장 후 Search Console 로 돌아가 **확인**. 보통 1~2분, 늦어도 한 시간입니다.

**4) 사이트맵 제출** — Search Console 왼쪽 **Sitemaps** →
`sitemap.xml` 입력 → **제출**. "성공"이 뜨면 됩니다.

```
https://susannadesign.co.kr/sitemap.xml
```

**5) 색인 요청 ★ 이게 실제로 순위를 당깁니다** — 맨 위 검색창(**URL 검사**)에
아래를 넣고 → **색인 생성 요청**. 이러면 구글이 며칠 안에 직접 찾아옵니다.

```
https://susannadesign.co.kr/
```

> **홈만 요청해도 됩니다.** 홈에서 다른 페이지로 링크가 걸려 있어 따라갑니다.
> 급하면 `/works` `/about` 정도만 더 요청하세요. 하루 요청 수에 한도가 있습니다.

**언제 나오나**: 색인 요청 후 보통 **2~7일**, 새 도메인이라 2주까지 걸릴 수도
있습니다. `site:susannadesign.co.kr` 로 검색해 결과가 나오면 성공입니다.
**구글 AI 가 이 사이트를 인용하려면 먼저 이 색인이 있어야 합니다.**

> 도메인 방식으로 소유확인을 하면 `config/site.ts` 의 `googleVerification` 은
> **비워 둬도 됩니다.** HTML 태그 방식을 고르셨을 때만 그 값이 필요합니다.

---

### C2 상세 — 구글 비즈니스 프로필 (⏳ 인증 대기)

**지식 패널(검색 우측 회사 카드)은 홈페이지 코드로 절대 못 만듭니다. GBP 가 유일한 경로입니다.**

> 🔴 **핵심 발견 (2026-08-06): 구글 지도에 이미 등록이 있었고, 정보가 틀린 채 노출 중이었습니다.**
>
> | | 발견 당시 | 주장하며 교정 |
> |---|---|---|
> | 카테고리 | `그래픽 디자이너` | **`간판제작업체`** |
> | 주소 | `유성구 원내동 98-1` | **`대전 서구 사기점골길 128`** |
> | 전화 | `042-541-0171` | **`010-7449-4600`** |
> | 우편번호 | `34227` | **`35425`** |
>
> 주 카테고리는 **로컬팩 랭킹의 가장 강한 신호**이고, 주소는 손님이 길찾기를 누르는 곳입니다.
> 네이버 플레이스가 "핀은 있는데 미인증" 이었던 것과 같은 패턴인데 **내용까지 틀렸다는 점이 더 나쁩니다.**

- 계정: `fivepeople201@gmail.com` (Search Console 도메인 소유확인과 같은 계정). 나중에 대표님 계정을 소유자로 추가할 수 있습니다.
- ⚠️ **이름은 `(주)수산나디자인` 그대로** — 공장 정면 간판 표기와 같고, 구글은 실제 간판 표기를 쓰라고 안내합니다.
  여기에 "대전간판" 같은 키워드를 붙이면 **프로필 정지 사유**입니다(당근만 예외 — NAP 절 참조).
- **인증은 구글이 방식을 지정합니다**(영상통화·엽서·전화·이메일). 자동화 불가, 엽서면 2주.
- 인증 후 채울 것: 사진 22장(`_place-upload/`) · 영업시간 평일 09:00~18:00 · 홈페이지 · 서비스 지역(대전 전역 + 세종·충청) · 소개글.

### C3 상세 — 네이버 서치어드바이저

**대전 지역 손님은 네이버에서 찾습니다.** 구글보다 이쪽이 매출에 직결됩니다.

1. [searchadvisor.naver.com](https://searchadvisor.naver.com) → 네이버 로그인
   → **웹마스터 도구** → 사이트 등록에 `https://susannadesign.co.kr` 입력
2. 소유확인 — ✅ **HTML 태그 방식으로 코드에 넣었습니다** (2026-07-28,
   `site.naverVerification`). **배포한 뒤** 서치어드바이저에서 `확인` 을 누르세요 —
   배포 전에 누르면 태그가 아직 없어 실패합니다.
   > 네이버가 먼저 권하는 **HTML 파일 업로드** 방식은 쓰지 않았습니다. "루트
   > 디렉토리에 업로드" 는 FTP 시절 표현이고, 이 사이트에서는 `public/` 에 파일을
   > 넣고 어차피 재배포해야 합니다. 배포 비용이 같다면 값이 `config/` 안에 모이는
   > 태그 방식이 낫습니다 [A5].
3. 확인되면 **요청 → 사이트맵 제출**: `sitemap.xml`
4. **요청 → RSS 제출**: `rss.xml` ← 네이버는 사이트맵과 RSS 를 **둘 다** 받습니다.
   시공사례가 늘 때마다 자동 반영됩니다
5. 등록이 끝나면 아래 IndexNow 가 네이버에도 실효를 갖게 됩니다

네이버는 색인까지 **14~16일**로 구글보다 느립니다. 먼저 걸어 두세요.

### ⚠️ NAP 일관성 — 주소·전화는 한 글자도 다르게 쓰지 않습니다

로컬 SEO의 기반입니다. 어긋나면 나머지 작업이 다 깎입니다. 기준값은 아래 그대로:

```
주소   대전광역시 서구 사기점골길 128
전화   010-7449-4600
홈페이지  https://susannadesign.co.kr        ← www 없는 형태
```

**상호 표기는 플랫폼 정책이 갈립니다 — 일부러 다릅니다.** NAP를 어긴 게 아니라,
어기지 않으려면 이렇게 밖에 못 씁니다.

| 채널 | 쓸 이름 | 근거 |
|---|---|---|
| 네이버 플레이스 · 구글 비즈니스 프로필 | **수산나디자인** | 실제 상호만. 서술어를 붙이면 GBP는 **프로필 정지 사유**입니다 |
| **당근 비즈프로필** | **수산나디자인 간판제작** ✅ 등록됨 | 당근은 매장명에 **서비스명을 포함하는 것을 공식 허용**합니다 |
| 숨고 | 수산나디자인 | 서비스 카테고리가 별도 필드라 상호에 넣을 필요 없음 |
| 홈페이지 구조화 데이터 | `수산나디자인` (`site.name`) + `주식회사 수산나디자인` (`legalName`) | `alternateName` 으로 둘 다 이미 냅니다 |

> **왜 이 정도 차이는 안전한가**: 검색엔진의 엔티티 매칭은 **주소·전화·홈페이지 URL**을
> 1차 키로 씁니다. 그 셋이 동일하고 상호의 **앞부분이 같으면** 같은 업체로 묶입니다.
> 반대로 상호를 통일하려고 GBP에 `수산나디자인 간판제작`을 넣으면 그건 정책 위반이라
> **프로필 자체가 날아갑니다.** 통일보다 각 플랫폼 정책 준수가 우선입니다.

**카테고리 주의** (C1·C2 공통): 나중에 바꾸기 까다롭습니다. 등록 전에 "대전 간판",
"대전 LED간판"을 실제로 검색해 상위 업체들이 쓰는 카테고리를 먼저 확인하세요.
`간판`·`간판제작` 같은 구체적인 것을 쓰고 `광고대행` 같은 포괄 카테고리는 피합니다.

**상호명에 키워드 금지** (C1·C2): "대전간판 1위" 같은 부가정보 삽입은 **프로필 정지 사유**입니다.
위 표에서 당근만 예외인 이유는 당근 정책이 명시적으로 허용하기 때문입니다 — 네이버·구글에
같은 방식을 쓰지 마세요.

---

### C1 상세 — 네이버 스마트플레이스 · 지도에서 홈페이지로 끌어오기

**대전 유입의 8할이 여기입니다.** 그런데 네이버는 자기 플랫폼에 손님을 머물게 하려
하므로, 홈페이지 유입은 **가만히 두면 거의 0** 입니다. 아래 3개를 채워야 길이 열립니다.

> ✅ **등록·사진·소개글 완료, 가격정보·영업시간·부가정보는 2026-08-06 에 직접 입력했습니다.**
> 플레이스 ID `1378209445` (관리 화면 bizId `11984815`).
>
> | 항목 | 상태 |
> |---|---|
> | 사진 · 소개글(603자) · 홈페이지/인스타 URL | ✅ 대표님이 입력 |
> | 가격정보 9건 (실측·시안 **무료** + 8건 변동가격) | ✅ 입력·저장 확인 |
> | 영업시간 평일 09:00~18:00 / 토·일·공휴일 휴무 | ✅ 입력·저장 확인 (`config/site.ts` 와 일치) |
> | 부가정보 방문접수/출장 · 예약 (주차 가능·무료는 기존값) | ✅ 입력·저장 확인 |
> | **대표키워드 (0개)** | 🔴 **기본정보가 "업체 검토중" 으로 잠겨 입력 불가** |
> | **노출 전화번호가 `042-541-0171`** | 🔴 홈페이지 대표번호(010)와 불일치 — 같은 이유로 잠김 |
> | 새소식 | ⬜ 초안 3편은 `_place-upload/네이버플레이스_등록내용.md` §8 |
>
> **검토가 끝나면 기본정보 화면에서 위 두 개(대표키워드·전화번호)를 바로 고치세요.**
> 기본정보는 일주일에 5회까지만 수정할 수 있으니 한 번에 같이 바꾸는 게 좋습니다.

### C1-b. 네이버 예약 — 검수 진행중 (2026-08-06 신청)

플레이스에 초록 **[예약]** 버튼을 붙이기 위한 별도 서비스입니다. 경쟁사
`아트통보너스디자인` 의 "간판제작 상담예약"과 같은 자리입니다.

| 항목 | 값 |
|---|---|
| 예약 사업자 ID | `1713161` (플레이스 `1378209445` 와 **다른 번호**) |
| 예약 유형 | **날짜 선택형** (손님이 날짜만 고름) — **변경 불가** |
| 상품 | `간판 무료 상담` · 0원 · **노출중** |
| 이용 | 월~금 09:00~18:00 / 토·일·공휴일 휴무 / 하루 3건 |
| 적는 칸 | **기타 요청사항**(자유 입력) 켬. 선택형 질문은 안 씀 |
| 검수 | ⏳ **진행중** — 영업일 2~3일, 결과는 `poing7003@naver.com` 메일 |

**설계 의도: 네이버는 "무엇을 하는지 + 적는 칸"만, 파일과 주소는 홈페이지.**
네이버 예약에는 파일 첨부가 없어서 상세소개에서 `susannadesign.co.kr/quote` 로 보냅니다.
우리 폼은 50MB×5개를 받아 알림 메일에 사진째 붙여 보내므로(F16·F20) 그쪽이 훨씬 낫습니다.

> ⚠️ **네이버 예약에 적은 내용은 우리 폼으로 넘어오지 않습니다.** 서로 다른 회사
> 시스템이고 네이버가 데이터를 밖으로 내보내지 않습니다. 손님이 두 번 적는 건
> 구조상 불가피합니다.

**검수 통과 후 남은 두 클릭** (`설정 → 운영설정`):
① `예약받기` 를 **시작** 으로 ② **`플레이스 연결하기`** — 이걸 눌러야 버튼이 붙습니다.

- ⚠️ **예약문의 번호가 `042` 입니다.** 그 칸은 `readonly` 라 예약 화면에서 못 고칩니다 —
  업체정보에서 끌어오는 값이라 **플레이스 기본정보를 `010` 으로 바꾸면 같이 바뀝니다.**
- ⚠️ **네이버 예약은 특수문자를 가립니다.** 허용: `- _ ( ) : & ! [ ] , . % + ~ @ * ^ ' / ? ² ℃ ※ < >`
  **가운뎃점 `·` 은 거부됩니다**(2026-08-06 홍보문구가 이것 때문에 막혔습니다). 문구를 고칠 때 주의.

**1) 등록** — [smartplace.naver.com](https://smartplace.naver.com) → 네이버 로그인
→ 업체 등록 → **사업자등록증 업로드**

> 사장님 본인이 사업자등록증으로 관리자 권한을 받은 업체가 노출 우선순위에서
> 유리한 경향이 있습니다. 대행업체 계정으로 만들지 마세요 — 나중에 소유권을
> 되찾는 게 번거롭습니다.

**2) 홈페이지 링크를 3곳에 넣습니다** ← 이게 유입의 실체

| 넣는 곳 | 값 | 효과 |
|---|---|---|
| **업체정보 → 홈페이지** | `https://susannadesign.co.kr` | 플레이스 상세에 "홈페이지" 버튼이 생깁니다 |
| **소개글 본문** | 주소를 문장 안에 한 번 | 버튼을 못 보는 사람용 |
| **새소식 게시글 하단** | 해당 시공사례 페이지 주소 | 글마다 유입 경로가 하나씩 늘어납니다 |

> ⚠️ **반드시 `www` 없는 형태**로 적으세요. `www` 로 적어도 301로 넘어가긴 하지만
> (F13), 리다이렉트를 한 번 타는 만큼 유입 추적과 신호가 흐려집니다.

**3) 채워야 노출됩니다** — 네이버 플레이스는 **정보 완성도가 곧 순위**입니다.

- 사진 **20장 이상** (외부 간판 시공 사진 위주 · 야간 점등 사진 필수)
- 대표 키워드 · 영업시간 · 주차 · 결제수단 · 편의시설 전부 채우기
- **새소식**: 시공 완료할 때마다 사진 3~5장 + 2~3문장. 방치하면 순위가 자동 하락합니다

**신규 플레이스는 90일간 노출 우대**를 받습니다. 등록 직후 이 세 개를 몰아서
채우는 게 1년 중 가장 효율이 좋은 구간입니다.

> **지도 앱 유입의 특성**: 플레이스에서 오는 사람은 이미 "대전 간판"을 검색한
> 사람입니다. 전환율이 검색 유입보다 훨씬 높습니다. 그래서 홈페이지 첫 화면의
> 전화 버튼과 간편 견적 폼(F5)이 중요합니다 — 이미 되어 있습니다.

---

### C5 상세 — 당근 비즈프로필 (✅ 등록 완료)

| 항목 | 값 |
|---|---|
| 이름 | **수산나디자인 간판제작** (최대 30자, 11자 사용) |
| 지역 | 대전 서구 — **한 달에 한 번만 변경 가능**하니 확인하고 고정 |
| 키워드 | `간판제작` `간판업체` `채널간판` `돌출간판` `LED간판` `간판교체` `간판수리` `옥외광고물` `아크릴간판` `캐노피` |
| 연락처 | `010-7449-4600` |
| 홈페이지 | `https://susannadesign.co.kr` |

> **키워드 입력 규칙**: 한글·영어·숫자만. **띄어쓰기·특수문자·이모지 불가**,
> 자음/모음만으로는 안 되고, 개당 **최대 20자**입니다.

**무료로 먼저 할 것 — 광고보다 이게 순서상 먼저입니다**

1. **사진** — 프로필이 비어 있으면 광고비가 그대로 새어 나갑니다. 클릭한 사람이
   도착하는 곳이 프로필입니다
2. **소식 발행** — 시공 사진 + 짧은 글. 소식은 중고거래 게시글 사이·동네지도 탭·
   검색 결과에 노출됩니다
3. **단골 확보** — 단골에게는 새 소식이 알림으로 갑니다. 광고 없이 반복 노출되는
   유일한 경로입니다

**당근 광고 (선택 · 유료)**

| 항목 | 내용 |
|---|---|
| 상품 | **디스플레이 광고**(동네 잠재고객) · **검색 광고**(검색 결과 상단, 간편모드) · 카탈로그 상품광고(전문가모드, 우리와 무관) |
| 과금 | **CPC** — 노출은 무료, 클릭해서 상세를 볼 때만 과금 |
| 단가 | 대략 **150~300원/클릭**. 수도권은 300~600원대. 전문가모드 최소 CPC 100원 |
| 타기팅 | 동네(동 단위) ~ 전국. **반경 타기팅**이 동 단위보다 클릭률 20% 높고 모객 비용 30% 낮았다는 게 당근 발표치 |

> **간판업은 반경 타기팅이 맞습니다.** 동 단위로 끊으면 길 하나 건너 상가가 빠집니다.
> 공장(서구 사기점골길) 기준이 아니라 **시공 가능 반경**으로 잡으세요.
>
> ⚠️ **프로필 채우기 전에 광고를 켜지 마세요.** 사진·후기 없는 프로필로 광고를
> 돌리면 클릭당 비용만 내고 문의는 안 옵니다.

**기대치**: 당근은 동네 B2C 기반이라 여기서 오는 건 **소규모 점포 간판·교체·수리**
문의입니다. 삼성화재·KAIST급 대형 사인 수주 경로가 아닙니다. 그래도 대전 서구·유성구
점포 물량은 실제로 잡힙니다.

---

### C6 상세 — 숨고 (등록 전에 비용 구조를 먼저 이해하세요)

숨고에는 **간판 제작 카테고리가 이미 있습니다.** 고객이 요청서를 쓰면 고수에게
매칭되고, 고수가 견적을 보내 경쟁합니다.

**비용 구조 — 여기가 핵심입니다**

| 항목 | 내용 |
|---|---|
| 최종 거래액 수수료 | **없음** — 계약 금액에서 떼가지 않습니다 |
| 견적 발송 | **유료.** 숨고캐시를 충전해 견적을 보낼 때마다 차감 |
| 과금 시점 | 고객이 **견적을 열람했을 때만** 부과 |
| 미열람 환급 | 발송 후 **48시간 내 미열람이면 보너스캐시로 반환** |

> 🔴 **이 구조가 뜻하는 것**: 낙찰이 안 돼도 **열람만으로 돈이 나갑니다.** 한 건에
> 여러 고수가 붙으므로, 아무 요청서에나 견적을 쏘면 캐시만 태웁니다.
>
> **선별 기준을 정해 두고 쓰세요** — ① 대전·세종·충청권 ② 예산대가 적힌 요청 ③
> 간판 종류가 특정된 요청. 이 셋을 만족하는 건에만 보냅니다.

**소액으로 시험한 뒤 판단하세요.** 캐시를 최소 단위만 충전해 10건 정도 보내고,
**계약 1건당 캐시 소모액**을 계산합니다. 그 값이 간판 1건 마진보다 크면 접으면 됩니다.
당근·플레이스와 달리 숨고는 **쓰는 만큼 즉시 비용**이라 손익 판단이 빠릅니다.

**숨고 프로필에도** 홈페이지 주소와 시공 사진을 넣습니다. 견적 경쟁에서 사진 있는
고수와 없는 고수의 차이가 가장 큽니다.

---

### 번개장터 — 우리 업종에는 해당 없습니다

당근과 같은 중고거래 앱이라 묶어 보기 쉽지만, 번개장터는 **브랜드 중고 물품 C2C**
플랫폼입니다. 광고 상품은 있으나 **상품(물건) 기반 타기팅**이고, 전문 판매자는
프로상점을 써야 하는 구조입니다 — **지역 서비스업 모객 지면이 아닙니다.**

간판은 물건이 아니라 현장 시공이므로 여기 예산을 쓸 이유가 없습니다.
**같은 돈이면 당근 반경 타기팅 또는 네이버 파워링크가 낫습니다.**

### 2주차~ — 운영

| # | 할 일 | 빈도 |
|---|---|---|
| C8 | 네이버 블로그 개설, "대전 간판" 주제로 시공 후기 포스팅 | **주 2회**, 최소 3~6개월 |
| C9 | 플레이스 사진·새소식 갱신 (방치하면 순위 자동 하락) | 월 1회 이상 |
| C10 | GBP 사진 업로드 + 게시물 | 사진 월 1회 / 게시물 주 1회 |
| C11 | **당근 소식 발행** — 시공 사진 + 2~3문장. 단골에게 알림이 갑니다 | 주 1회 |
| C12 | **숨고 요청서 확인** — C6의 선별 기준에 맞는 건에만 견적 발송 | 매일 (발송은 선별) |
| C13 | 지식인 답변 (정보 80% : 업체 언급 20%) | 주 1~2건 |
| C14 | 리뷰 요청 — 실제 시공 고객에게만, 내용 강요 없이 | 시공 완료 시 |
| C15 | 리뷰 응답 | 24~48시간 이내 |

**한 주 커리큘럼 (실제로 돌릴 수 있는 최소치)**

| 요일 | 할 일 | 소요 |
|---|---|---|
| 월 | 숨고 요청서 확인 · 당근 소식 1건 발행 | 20분 |
| 수 | **블로그 글 1편** (시공 후기) | 60~90분 |
| 금 | **블로그 글 1편** · 플레이스 새소식 1건 | 60~90분 |
| 시공 완료 시 | 사진 촬영 → 리뷰 요청 → 플레이스·당근·블로그에 각각 올림 | 30분 |

> 사진 한 번 찍으면 **블로그·플레이스·당근·홈페이지 실적** 네 곳에 쓰입니다.
> 촬영이 병목이지 글쓰기가 병목이 아닙니다. 현장 나갈 때 **점등 후 야간 사진**을
> 꼭 같이 찍어 두세요 — 간판업에서 가장 잘 먹히는 사진입니다.

---

### C8 상세 — 네이버 블로그 (네이버 노출의 절반)

**2026년 로직 — 무엇이 바뀌었나**

| 알고리즘 | 무엇을 보나 | 우리에게 뜻하는 것 |
|---|---|---|
| **C-Rank** | 이 블로그가 **특정 분야에서 얼마나 전문적인가** | 한 카테고리에 글이 쌓여야 인정됩니다. 통설은 **최소 50편**. 간판 글만 계속 쓰는 게 맞고, 맛집·일상 글을 섞으면 전문성이 희석됩니다 |
| **D.I.A.+** | 문서 자체의 **경험·구체성** | 일반론 복사는 안 먹힙니다 |
| **하이퍼클로바X** | 네이버가 검색에 본격 적용한 자체 AI | 키워드 빈도·글자수 같은 기술적 요소의 비중이 줄고 **문맥의 진정성**이 핵심이 됐습니다 |

> **가장 실무적인 결론**: **숫자를 적으세요.** AI가 신뢰하는 건 애매한 표현이 아니라
> 구체적 수치입니다. "크게 만들었어요"가 아니라 **"가로 6m 채널간판, 3층 높이라
> 25톤 고소작업차 투입, 제작 5일 시공 반나절"** 처럼 씁니다.
> 이건 간판업이 절대적으로 유리한 지점입니다 — 치수·자재·장비·기간이 원래 다 숫자입니다.

**글 하나의 실무 기준**

- 제목 25~30자, **키워드 앞배치** — `대전 서구 카페 채널간판 제작 시공 후기`
- 본문 1,500~2,500자 · 이미지 6~13장 · 문단 4~5줄
- 세일즈 문구는 20% 이내, 태그 5개 이내
- 같은 사진 재사용 시 **반드시 편집** (자르기·보정)
- 구조: 현장 상황 → 문제 → 어떻게 풀었나(치수·자재·장비) → 결과 사진 → 비용 대략

> ⚠️ **자사 홈페이지 링크를 매 글에 반복 삽입하지 마세요.** 네이버는 자사 플랫폼
> 체류를 선호해 제재 대상입니다. 프로필·소개란에 한 번 걸고, 본문에는 필요한 글에만
> 자연스럽게 넣습니다. **홈페이지 유입은 블로그가 아니라 플레이스(C1)로 뚫습니다.**

**블로그를 열면 알려주세요** — `config/site.ts` 의 `blogUrl` 에 넣으면 구조화 데이터
`sameAs` 가 한 겹 두꺼워집니다 (E 표 4번).

---

### 유료 광고 — 어디에 먼저 쓸까

**전부 선택입니다.** 무료로 할 수 있는 C1·C5·C8을 채우기 전에 광고를 켜면 돈만 나갑니다.

| 채널 | 과금 | 대전 간판업 적합도 | 판단 |
|---|---|---|---|
| **당근 광고** | CPC 150~300원 | **높음** — 동네 점포 간판 문의 | ★ 첫 광고로 적합. **반경 타기팅**으로 |
| **네이버 파워링크** | CPC (키워드별 상이) | **높음** — 구매 의도가 가장 뚜렷 | ★ 롱테일 위주 소액. 확장검색(자동 매치)은 끄고 직접 등록 |
| **숨고 캐시** | 견적 열람당 | 중간 — 낙찰 안 돼도 과금 | 소액 시험 후 계약당 단가로 판단 (C6) |
| 번개장터 | — | **없음** | 물건 기반 플랫폼. 하지 않습니다 |

파워링크는 초기 3~6개월만 롱테일 위주 소액 운영 → 오가닉이 안정되면 브랜드 키워드
방어로 축소하는 게 정석입니다.

---

## D. 절대 하면 안 되는 것

| 채널 | 행위 | 결과 |
|---|---|---|
| 플레이스 | 리뷰에 별점·키워드 강요, 허위 영수증 | 계정·업체 이용 제한 |
| 플레이스 | 등록 후 방치 | 순위 자동 하락 |
| 블로그 | 키워드 반복, 제목-내용 불일치, 이미지 재사용 | 저품질 전환 |
| 블로그 | 방문자수·체류시간 조작 프로그램 | 검색 제외 |
| 지식인 | 자문자답 | 계정 정지 |
| 카페 | 동일 링크 도배 | 강퇴·노출 제한 |
| GBP | 상호명에 키워드 삽입 | 프로필 정지 |
| GBP | 리뷰 인센티브 제공, 직원 실명 태깅 요청 | 리뷰 자동 삭제 → 반복 시 정지 |
| 웹사이트 | 지명만 바꾼 지역 페이지 | 코어 업데이트 페널티 |
| 웹사이트 | 타사 리뷰를 스키마에 도용 | 리치 결과 제외 |
| 공통 | "상위노출 대행" 순위 조작 구매 | 채널 전체 페널티 |

2025~2026년 양쪽 모두 단속을 강화하는 추세입니다. 대행업체가 파는
"플레이스 상위노출", "카페 지수 작업"은 적발 시 계정 전체가 날아갑니다.

---

## E. 남은 코드 작업

| 우선순위 | 항목 | 막고 있는 것 |
|---|---|---|
| **1** | **시공사례 개별 페이지** (B-1) — 효과가 가장 큼 | 사례별 상세 내용 |
| **2** | **사진 투입** + 지역·업종 담긴 `alt` (홈 35자리 · `work-01`~`15`) | 사진 |
| ~~3~~ | ✅ **좌표 확정** (2026-08-06) — 네이버 플레이스 등록 좌표 `36.2341389, 127.3201981` 로 교체. 이전 근사값은 실제 위치에서 **약 10km** 벗어나 있어 로컬팩 "거리" 요인에 계속 불리하게 작용하고 있었습니다 | — |
| ~~4~~ | ✅ **`site.naverPlaceUrl` 신설 + `sameAs` 연결** (2026-08-06) — 플레이스(C1)와 홈페이지가 같은 회사로 묶입니다 | — |
| 4 | `site.daangnUrl` **신설** + `sameAs` 연결 — 당근 비즈프로필(C5)이 홈페이지와 같은 회사로 묶입니다 | **당근 프로필 URL** |
| 5 | `site.blogUrl` — 네이버 블로그(C8)를 열면 `sameAs` 가 한 겹 두꺼워집니다 | 블로그 개설 |
| 6 | 우편번호·운영시간 확정 | 확인 |

> **소유확인 코드는 둘 다 끝났습니다.** 네이버는 HTML 태그 방식으로
> `site.naverVerification` 에 넣었고(2026-07-28), 구글은 **도메인(DNS TXT) 방식**이라
> 코드에 넣을 값이 애초에 없습니다.

### IndexNow — 계정 없이 색인 통보 (F17)

```bash
npm run indexnow
```

운영 `sitemap.xml` 의 전 주소를 참여 검색엔진에 **즉시 통보**합니다.
소유확인은 `/indexnow.txt` 로 하므로 **계정도 로그인도 필요 없습니다.**

| 검색엔진 | 참여 | 비고 |
|---|---|---|
| **Bing** | ✅ | **ChatGPT·Perplexity·Copilot 이 읽는 인덱스** — 등록 없이 접수 확인(202) |
| **네이버** | ✅ | **해결 (2026-07-28, HTTP 200).** 소유확인 전에는 같은 키로 Bing 만 202 이고 네이버는 403 이었습니다 — 키 문제가 아니라 서치어드바이저에 사이트가 없어서였습니다. **네이버 403 은 키를 의심하기 전에 등록 여부를 먼저 보세요** |
| Yandex · Seznam · Yep · Internet Archive · Amazonbot | ✅ | |
| **구글** | ❌ **미참여** | Search Console(C4) 외에 길이 없습니다 |

참여사 목록은 `https://www.indexnow.org/searchengines.json` 에서 실측 확인했습니다.

**언제 다시 실행하나**: 페이지를 새로 만들거나 실적을 크게 늘린 뒤. 매일 돌릴
필요는 없고, 같은 주소를 반복 제출하면 429(너무 잦음)가 납니다.

### 검색 결과를 "공식처럼" 보이게 만드는 요소 — 무엇이 코드고 무엇이 아닌가

대표님이 물으신 "구글 검색하면 이쁘게 여러 가지 보이게" 를 항목별로 갈랐습니다.
**대부분은 코드로 만드는 게 아니라 구글이 스스로 붙여 주는 것**이라 구분이 중요합니다.

| 요소 | 지금 | 코드로 되나 | 무엇이 필요한가 |
|---|---|---|---|
| **파비콘**(주소 옆 로고) | ⏳ 기본 지구본 | **이미 다 됨** | 512×512 정사각 PNG + `<link rel="icon">` 정상. 구글 문서상 **재크롤링에 며칠~몇 주**. 기다리는 것 외에 할 게 없음 |
| **사이트 이름** | ✅ 조치함 | **예** | `WebSite` 구조화 데이터를 홈에 넣었습니다(F11). 이전엔 도메인이 그대로 나왔습니다 |
| **설명문** | ✅ 정상 | 예 | `seo.homeDescription` 이 그대로 나옵니다 |
| **사이트링크**(하위 링크 4~6개) | ❌ 없음 | **아니오** | 구글이 **자동 생성**합니다. 요청 불가. 사이트 구조·내부링크·트래픽이 쌓여야 붙습니다 |
| **지식 패널**(우측 회사 카드) | ❌ 없음 | **아니오** | **구글 비즈니스 프로필(C2)** 이 사실상 유일한 경로입니다. 홈페이지 코드로는 안 만들어집니다 |
| **별점** | ❌ 없음 | ⛔ **넣으면 안 됨** | 자사 게시 리뷰에만 허용. 플레이스·GBP 리뷰를 스키마에 넣으면 **리치 결과 제외 또는 수동 조치** (§D) |
| **AI 개요 인용** | ⚠️ 디렉터리만 | 일부 | 위 "문제 2" 참조 |

> **핵심**: "공식처럼 보이는" 카드(지식 패널)는 **홈페이지가 아니라 구글 비즈니스
> 프로필이 만듭니다.** C2 를 하시면 로고·사진·영업시간·전화·리뷰가 담긴 카드가
> 검색 우측에 붙습니다. 코드로 할 수 있는 건 이미 다 해 뒀습니다.

### 배포 후 실측 — 기술 SEO 상태 (2026-07-27 갱신)

| 항목 | 결과 |
|---|---|
| `robots.txt` 검색 허용 | ✅ (`isProductionDomain=true`, `/api/` `/admin` 만 차단) |
| `sitemap.xml` | ✅ 전 주소가 비`www` 확정 도메인 |
| `canonical` · `og:url` | ✅ `www` 로 접속해도 비`www` 를 가리킴 |
| `www` → 비`www` **301** | ✅ **배포 확인 완료** — 루트·다단경로·쿼리스트링 전부 301 (F13) |
| 크롤러 실접근 | ✅ Googlebot 등 **8종 전부 200** (UA 실측, 차단 0건) |
| `<meta name="robots">` | ✅ `index, follow` (`noindex` 3개는 의도대로 `noindex, follow`) |
| 설명문 길이 · 페이지별 OG | ✅ **운영 10페이지 실측 (2026-07-28)** — 전부 43~62자, `description`=`og:description` 일치, `og:image` 유지 |
| `sitemap.xml` 건수 | ✅ **7건** — `noindex` 3개 빠진 것 확인 |
| 사진 `alt` 자산 | ⚠️ 플레이스홀더 **홈 35자리 · `/works` 15자리** (사진 6장 투입 후) — E 표 3번 |
| 구글 색인 | ⚠️ **홈 1페이지만** — 사이트맵 10개 중 1개. 문서 맨 위 "현재 색인 상태" 참조 |
| `http` → `https` 301 | ✅ **해결** (Cloudflare Always Use HTTPS) |
| `sameAs` (엔티티 연결) | ⚠️ 인스타그램만 연결. **당근 비즈프로필(C5) 미연결** — URL 확보 후 `daangnUrl` 신설 필요. `blogUrl`·`kakaoChannelUrl` 도 공란 |
| `site.bizNo` | ✅ 입력 — 푸터 + 구조화 데이터 `taxID` 양쪽에 나갑니다 |

### AI 답변 엔진 인용 — 크롤러는 이미 열려 있습니다

**목표**: "대전 간판업체 추천해줘" 같은 질문에 ChatGPT·Perplexity·구글 AI 개요가
이 사이트를 인용하게 하는 것.

접근 자체는 **두 겹으로 열려 있습니다.** Cloudflare 관리형 `robots.txt` 블록이
해제됐고, `app/robots.ts` 가 AI 크롤러 14종을 이름으로 지목해 `Allow` 를 선언합니다
(F15). 실측상 AI 봇에 걸린 `Disallow: /` 가 하나도 없고 UA 8종이 전부 200 입니다.

> ⚠️ **`app/robots.ts` 를 고칠 때** — AI 그룹에도 `/api/` `/admin` 차단이 반드시
> 같이 들어 있어야 합니다. 크롤러는 자기 이름의 그룹이 있으면 `*` 그룹을 아예
> 보지 않으므로, 빼먹으면 **이 봇들에게만 관리자 화면이 열립니다.** `POLICY` 를
> 두 그룹이 공유하는 구조가 그래서 중요합니다 — 한쪽만 고치지 마세요.

> **Cloudflare 가 기본값을 다시 켜더라도** 한 겹 버팁니다. robots.txt 는 같은
> User-agent 그룹끼리 합쳐지고, 동일 구체성의 Allow/Disallow 충돌 시 Allow 가
> 이깁니다(RFC 9309 · 구글 문서).

**알고 계실 트레이드오프**: 같은 봇들이 콘텐츠를 **AI 학습에도** 씁니다
(`GPTBot` `Google-Extended` `Applebot-Extended` 등). 이 사이트는 회사 소개·시공실적
이라 학습에 쓰여도 잃을 게 거의 없고 인용 유입이 더 큰 이득입니다. 생각이 바뀌면
Cloudflare 대시보드 → **AI Crawl Control** 에서 되돌리고, 그때 `app/robots.ts` 의
`AI_CRAWLERS` 배열도 함께 비웁니다.

**그래서 지금 부족한 건 접근이 아니라 콘텐츠입니다** — 위 "문제 2" 참조.

### 도메인 표기 규칙

`site.url = https://susannadesign.co.kr` (**www 없음**)으로 확정했습니다.
sitemap · RSS · 구조화 데이터 · canonical · OG 가 전부 이 값에서 나옵니다.

`www` 와 `http` 로 들어와도 전부 이 주소로 301 됩니다 — 방식과 주의점은
[`ARCHITECTURE.md`](ARCHITECTURE.md) **F13** (`www` 는 코드, `http` 는 Cloudflare).

C1~C7 채널 등록 시 홈페이지 주소도 **www 없는 형태**로 통일해 적으세요.
당근·플레이스·숨고 프로필의 홈페이지 칸도 같습니다.
