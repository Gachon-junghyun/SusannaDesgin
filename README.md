# 수산나디자인 홈페이지

간판 제작·시공 기업 홈페이지. Next.js 16 (App Router) · React 19 · TypeScript · Tailwind CSS v4.

> ### 🌐 https://susannadesign.co.kr — **운영 중**
> Cloudflare Workers 배포 완료, Supabase 연결됨, 관리자 화면(`/admin`) 동작 확인.
> 구글에 "수산나디자인" 검색 시 **1위 노출 중**.
> **급한 일은 ① 시공사진 ② 네이버 등록** — 아래 [지금 해야 할 일](#지금-해야-할-일) 참조.

## 실행

```bash
npm run dev
```

http://localhost:3000

```bash
npm run build   # 프로덕션 빌드
npm run lint    # 린트
```

---

## 브랜드

회사소개서(`수산나디자인3.pdf`, `회사소개서_20240429.pdf`)에서 추출했습니다.

| | 값 | 쓰임 |
|---|---|---|
| Brand (청록) | `#00A79D` | 버튼·링크·활성 상태·CTA 밴드. 신자료 표지 `#00AFA2`, 구자료 `#00A79D` 중 공통값 |
| Accent (주황) | `#FF5900` | 섹션 eyebrow·필수표시·에러. 구자료 표지 국문 헤드라인 색 |
| Paper | `#F5F5F2` | 본문 배경. 신자료 내지 배경 |
| Ink | `#0F1A19` | 다크 섹션. 청록 쪽으로 살짝 기울인 먹색 |

토큰은 `app/globals.css` 의 `@theme` 블록에 있습니다.

**로고는 벡터(SVG)로 변환해 두었습니다.** PDF 안에 벡터 원본이 없어서(임베드 래스터뿐)
가장 해상도 높은 래스터를 potrace로 트레이싱했습니다. → `public/logo.svg`, `public/logo-white.svg`

> ⚠️ 원본 AI/EPS 파일이 있으면 그걸로 교체하세요. 현재 SVG는 저해상도 래스터를 되살린
> 것이라 곡선이 원본 설계와 미세하게 다릅니다. 큰 사이즈(현수막·차량 랩핑)로 쓰기엔
> 원본이 필요합니다. 웹에서 쓰는 크기(헤더 36px, 푸터 32px)에서는 차이가 보이지 않습니다.

---

## 관리자 기능 켜기 (Supabase)

첫 화면 사진과 주요 실적을 **코드 수정·재배포 없이** 브라우저에서 직접 바꾸는 기능입니다.
설정 전에도 공개 홈페이지는 지금 그대로 동작하니, 급하지 않으면 나중에 하셔도 됩니다.

> **📘 화면 그대로 따라가는 안내서: [`docs/SUPABASE-SETUP.md`](docs/SUPABASE-SETUP.md)** (15분)
> 막히면 `npm run supabase:check` 로 무엇이 잘못됐는지 확인할 수 있습니다.
> 아래는 요약입니다.

### 1. Supabase 프로젝트 만들기

[supabase.com](https://supabase.com) 에서 프로젝트를 하나 만듭니다 (무료 플랜으로 충분합니다).
지역은 **Northeast Asia (Seoul)** 을 고르세요. 멀수록 관리 화면이 느려집니다.

> 계정 생성과 비밀번호 입력은 직접 하셔야 합니다.

### 2. 테이블 만들기

대시보드 왼쪽 **SQL Editor** 에서 [`supabase/migrations/`](supabase/migrations/) 안의 파일을
번호 순서대로 하나씩 붙여넣고 **RUN**. 여러 번 실행해도 중복으로 쌓이지 않습니다.

| 파일 | 무엇이 생기나 |
|---|---|
| `0001_init.sql` | 테이블·권한 정책·사진 보관함 + 슬라이드 3개·실적 15건 |
| `0002_quotes.sql` | 견적 문의함 |
| `0003_works_photos.sql` | 사진 있는 실적 6건 |
| `0004_works_brochure.sql` | 실적 2건 추가 + 사진 없는 건을 뒤로 |
| `0005_content_blocks.sql` | **페이지 문구** — 아래 “페이지 문구 바꾸기” 메뉴가 이걸로 돕니다 |
| `0006_quote_files.sql` | **견적 문의 첨부 사진 보관함** — 이걸 실행해야 손님이 올린 사진이 남습니다 |

> 안 돌려도 홈페이지는 멀쩡합니다. 그 경우 코드에 들어 있는 지금 문구가 그대로
> 나가고, 관리자 화면에만 “실행하세요” 안내가 뜹니다.

### 3. 접속 정보 넣기

**Project Settings → API** 에서 두 값을 복사해 `.env.local` 파일에 넣습니다.
([`.env.local.example`](.env.local.example) 을 복사해서 쓰세요.)

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...
```

넣은 뒤 개발 서버를 **다시 시작**해야 반영됩니다.

> `service_role` 키는 쓰지 않습니다. 그 키는 모든 권한 검사를 무시하므로 절대 넣지 마세요.

### 4. 관리자 계정 만들기

대시보드 **Authentication → Users → Add user** 로 이메일·비밀번호 계정을 만듭니다.

**가장 먼저 만든 계정이 자동으로 관리자가 됩니다.** 그 다음부터 만들어지는 계정은
권한 없는 일반 계정이 되니, 계정을 만든 직후 **Authentication → Providers → Email** 에서
회원가입(Enable sign-ups)을 꺼 두세요. 아무나 가입해서 계정이 늘어나는 걸 막습니다.

이제 `/admin` 으로 들어가 로그인하면 됩니다.

### 무엇을 바꿀 수 있나

| 메뉴 | 할 수 있는 일 |
|---|---|
| 견적 문의 | 들어온 문의 확인·확인처리·삭제. **맨 위에 알림 메일이 어디로 가는지 표시됩니다** |
| 첫 화면 사진 | 사진 추가·교체·삭제, 순서 바꾸기, 문구 수정, 잠시 숨기기 |
| 주요 실적 | 카드 추가·삭제, 사진·업종·지역·태그 수정, 순서 바꾸기, 숨기기 |
| **페이지 문구** | 아래 표 참고 |

저장하면 공개 페이지가 즉시 다시 만들어집니다(`revalidatePath`). 배포할 필요 없습니다.

#### 페이지 문구 — `/admin/content`

위쪽 탭으로 구역을 고르고 고칩니다. **사진도 여기서 바꿉니다.**

| 탭 | 화면 어디 | 예 |
|---|---|---|
| 구역 제목 | 홈 각 구역의 큰 문구 | “공장을 가졌다는 건 / 일정을 지킬 수 있다는 뜻입니다” |
| 회사 강점 | 홈 WHY SUSANNA 의 점 네 줄 | “795평 자체 공장” |
| 업무 프로세스 | 홈 PROCESS + 업무프로세스 페이지 | 01 상담 및 접수 … 05 시공 및 완료 |
| 제작 공정 | 홈 검은 배경 여섯 칸 + 회사소개 | 절단 · 가공 · 도색 · 조립 · 설치 · 검수 |
| 사업영역 | 홈 맨 위 네 칸 + 사업영역 페이지 | 옥외광고물 · 간판디자인 · 철구조물 · 옥내광고물 |

- **큰 제목의 줄바꿈은 화면에서도 그대로 나뉩니다.** 두 줄로 보이길 원하면 두 줄로 적으세요.
- **“구역 제목” 탭만 항목을 늘리거나 지울 수 없습니다.** 화면에 자리가 정해져 있어서,
  새로 넣어도 어디에도 안 나오고 지우면 그 구역 제목이 통째로 사라지기 때문입니다.
  내용만 고치면 됩니다.
- 나머지 탭은 **추가·삭제·순서 바꾸기**가 다 됩니다. 지우기 전에 “홈페이지에 보이기”
  체크를 풀어 숨겨 보는 편이 안전합니다. 삭제는 되돌릴 수 없습니다.
- ⚠️ **한 탭의 항목을 전부 지워서 그 구역을 없앨 수는 없습니다.** 0건이 되는 순간
  사이트가 **기본 내용으로 되돌아갑니다** — 홈페이지가 빈 화면이 되지 않게 하려고
  일부러 그렇게 만들어 두었습니다. 구역을 통째로 없애는 건 코드 작업입니다.
- 한 항목을 고치면 그게 나오는 **모든 페이지가 같이** 바뀝니다(예: 제작 공정은 홈과
  회사소개 양쪽).

### 안전장치

- **권한은 DB 가 판단합니다.** 테이블마다 RLS 정책이 걸려 있어, 코드에 실수가 있어도
  관리자가 아닌 사람은 데이터를 고칠 수 없습니다. 화면에서 막는 건 1차 필터일 뿐입니다.
- **서버 액션마다 권한을 다시 확인합니다.** 서버 액션은 화면과 별개로 인터넷에 열린
  엔드포인트라, 화면에서만 막으면 그대로 호출할 수 있기 때문입니다.
- **관리자 화면은 미리 구워지지 않습니다** (`dynamic = "force-dynamic"`) — 내용이 정적
  파일로 굳어 새어 나가는 걸 막습니다. 검색엔진 수집도 막아 뒀습니다(`noindex`).
- **사진은 브라우저에서 스토리지로 곧장 올라갑니다.** 서버 액션은 본문 한도가 1MB 라
  사진을 못 넘깁니다. 업로드 권한 역시 스토리지 정책이 판단합니다.
- **Supabase 가 죽어도 홈페이지는 살아 있습니다.** DB 를 못 읽으면 `config/content.ts`
  내용으로 자동 대체됩니다. 회사 홈페이지가 빈 화면이 되는 상황을 만들지 않습니다.
- **세션 확인은 `/admin` 에서만 합니다.** 전체 경로에 걸면 방문자마다 인증 서버를
  거치느라 공개 페이지가 느려집니다.

### 사진은 어디에 저장되나

| 경로 형태 | 의미 |
|---|---|
| `/images/hero-night.jpg` | `public/images/` 에 직접 넣은 파일 |
| `https://xxxxx.supabase.co/storage/...` | 관리자 화면에서 올린 파일 |

둘 다 섞어 쓸 수 있습니다. 기존 사진은 그대로 두고 필요한 것만 관리 화면에서 교체하면 됩니다.

### 화면 대신 명령으로 관리하기 — `npm run cms`

사진 여러 장을 한꺼번에 실적으로 넣을 때는 화면에서 장당 여러 번 클릭해야 합니다.
같은 일을 명령 한 줄로 하는 도구가 있습니다. **AI 에게 사진을 주고 정리를 맡길 때도
이 도구를 씁니다** — 브라우저 로그인이 필요한 화면으로는 못 하는 일입니다.

`.env.local` 에 관리자 계정을 넣어야 켜집니다 (관리자 화면에 쓰는 그 계정입니다).

```bash
SUPABASE_ADMIN_EMAIL=you@example.com
SUPABASE_ADMIN_PASSWORD=비밀번호
```

```bash
npm run cms -- help
```

자주 쓰는 것:

| 명령 | 하는 일 |
|---|---|
| `npm run cms -- list works` | 실적 전부 보기 (숨긴 것 포함) |
| `npm run cms -- upload ./사진.jpg` | 사진만 올리고 주소 받기 |
| `npm run cms -- works add --image ./사진.jpg --title "태평한우" --category "상업시설"` | 실적 한 건 등록 |
| `npm run cms -- works batch 계획.json` | 사진 여러 장을 한 번에 등록 |
| `npm run cms -- works renumber` | 순서가 안 바뀔 때 순서값 정리 |

- **배포할 필요 없습니다.** 바꾸면 홈페이지에 바로 반영됩니다.
- **권한은 관리 화면과 똑같습니다.** 관리자 계정이 아니면 아무것도 못 합니다.
- **삭제는 `--yes` 를 붙여야 실행됩니다.** 안 붙이면 지울 대상만 보여줍니다.
- 견적 문의는 **건수만** 나옵니다. 고객 이름·연락처가 터미널에 남지 않게 한 것으로,
  내용은 `/admin/quotes` 에서 보세요.
- ⚠️ 비밀번호가 `.env.local` 에 그대로 적힙니다. git 에는 올라가지 않지만
  화면 공유·녹화할 때는 가리세요.

---

## 지금 해야 할 일

사이트는 이미 인터넷에 올라가 **고객이 볼 수 있는 상태**입니다.
1번은 지금 방문하는 고객 눈에 그대로 보이는 문제고, 2번은 그 고객이 애초에
찾아오게 만드는 일이라 둘이 가장 급합니다.

> 코드 쪽에 남은 일 전체 목록은 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) **§7** 에 있습니다.
> 여기 1~4번은 그중 **대표님이 직접 하셔야 하는 것**만 추린 것입니다.

### 🔴 1. 사진 넣기 → `public/images/` ★ 가장 급합니다

**운영 사이트에 "사진 준비 중" 박스가 홈 35자리 · 시공사례 15자리 떠 있습니다.**
(2026-07-27 에 사업영역 3칸 + 실적 6건이 실사진으로 채워진 뒤 남은 수입니다.)
간판업체 홈페이지에서 시공사진이 없는 건 사실상 빈 사이트입니다.

> 박스에 `work-01.jpg` `1200×900` 같은 개발자용 정보가 찍혀 나오던 문제는 고쳤습니다 —
> 이제 운영 화면에서는 실적 이름과 "사진 준비 중" 만 조용히 보입니다. 보기에 덜
> 어색해진 것뿐이고, **사진 자체는 여전히 필요합니다.**

**파일명만 맞춰서 넣으면 코드 수정 없이 자동 교체됩니다.**
필요한 목록과 크기는 [`public/images/README.md`](public/images/README.md) — 총 30장 내외입니다.

> 급하면 관리 화면(`/admin`)에서 올리는 방법도 있습니다. 첫 화면 사진 1장은 이미
> 그렇게 올려 두셨고 정상 서비스되고 있습니다. 다만 시공사례 15건은 `public/images/`
> 쪽이 한 번에 넣기 편합니다.

### 🔴 2. 네이버에 등록하기 ★ 구글은 이미 나옵니다

> **📸 2026-08-05 실측 (네이버·구글 직접 검색으로 확인) — 아래 표보다 이게 최신입니다.**
> - **네이버 웹 색인은 이미 끝났습니다.** "수산나디자인" 검색 시 홈페이지가 **오가닉 1위 +
>   사이트링크**(사업영역·회사소개·주요실적·업무프로세스·고객지원·견적문의)로 뜹니다.
>   `/about` `/signs` `/works` `/process` `/support` `/quote` 전부 색인됨 → **서치어드바이저
>   사이트맵 제출은 사실상 반영 완료.** 아래 표의 "색인 14~16일 대기"는 지난 얘기입니다.
> - **네이버 플레이스 핀도 이미 있습니다** — 지도에 "수산나디자인", 042-541-0171,
>   사기점골길 128(평촌동 60-2), 업종 "옥외,전시광고". **다만 미인증 상태**(플레이스 박스에
>   "내 업체 등록" 링크가 뜸). 즉 진짜 남은 일은 *등록*이 아니라 **스마트플레이스 소유권
>   claim + 사진·소개·영업시간으로 꾸미기**입니다. 이건 대표님 계정 로그인·본인확인이 필요.
> - **구글 하위페이지 색인은 아직 얕습니다** — `site:susannadesign.co.kr` 기준 **`/`와
>   `/signs` 2개만** 색인됨. `/works` `/about` `/process` `/support` 는 아직 안 들어감
>   → 아래 "#### 구글 쪽에 남은 것" 1번(Search Console 색인 요청)이 여전히 유효합니다.

**구글에 "수산나디자인" 을 치면 우리 홈페이지가 1위로 나옵니다.**
검색엔진 쪽 코드는 손볼 게 남아 있지 않습니다.

> ### ✅ 네이버 스마트플레이스 — 대부분 채웠습니다 (2026-08-06)
>
> 사진·소개글은 대표님이 넣으셨고, **가격정보 9건 · 영업시간 · 부가정보는 제가 직접
> 입력해 저장까지 확인**했습니다. 플레이스 주소는 아래입니다.
>
> **https://map.naver.com/p/entry/place/1378209445**
>
> 홈페이지에도 물려 뒀습니다 — 검색엔진이 이제 플레이스와 홈페이지를 **같은 회사로**
> 인식합니다. 등록 좌표를 받아 왔더니 코드에 있던 값이 실제 위치에서 **10km쯤
> 벗어나 있어** 그것도 바로잡았습니다(지역 검색에서 "거리"가 순위 요인이라 그동안
> 손해를 보고 있었습니다).
>
> **고객지원 → 오시는길**도 채웠습니다. 네이버가 자기 지도를 남의 사이트 안에 끼워
> 넣는 걸 막아 놔서(`x-frame-options`) 약도 그림은 못 넣지만, 대신 **주소 · 영업시간 +
> `네이버 지도에서 보기` · `카카오맵 길찾기` · `전화` 버튼**을 넣었습니다.
> 손님이 실제로 누르는 건 약도가 아니라 길찾기이고, 네이버 지도 버튼은 플레이스로
> 이어져서 **플레이스 노출에도 도움**이 됩니다.
>
> 진짜 움직이는 지도를 원하시면 카카오 개발자 키(무료)만 발급해 주시면 넣어 드립니다.
>
> #### 🔴 검토가 끝나면 두 가지만 손봐 주세요
>
> 신규 등록 직후엔 네이버가 **기본정보를 "검토중" 으로 잠급니다.** 그래서 아래 둘은
> 지금 못 고칩니다. 검토 완료 후 **스마트플레이스 → 업체정보 → 기본정보**에서:
>
> 1. **전화번호를 `010-7449-4600` 으로** — 지금 플레이스엔 `042-541-0171` 이 떠 있습니다.
>    홈페이지·당근과 번호가 다르면 검색엔진이 같은 업체로 묶는 신호가 약해집니다.
> 2. **대표키워드 5개 입력** — 지금 0개입니다.
>    `대전간판` `대전간판제작` `옥외광고물` `채널간판` `철구조물`
>
> 기본정보는 **일주일에 5회까지만** 수정되니 두 개를 한 번에 바꾸세요.
>
> 남은 건 **새소식**입니다(초안 3편: `_place-upload/네이버플레이스_등록내용.md`).
> 신규 플레이스는 **90일간 노출 우대**를 받아서, 지금 채우는 게 1년 중 가장 효율이 좋습니다.

> ### ⏳ 네이버 예약 — 검수 신청 완료, 결과 대기중 (2026-08-06)
>
> 플레이스에 초록 **[예약]** 버튼을 붙이는 작업입니다. 상품 하나를 만들어 두었습니다.
>
> **`간판 무료 상담`** · 무료 · 월~금 09:00~18:00 (토·일·공휴일 휴무) · 하루 3건
>
> 손님이 **날짜를 고르고**, 자유 입력칸에 요청사항을 적는 방식입니다.
> 상품 설명에는 **우리가 뭘 하는지 목록**과, **사진·도면은 홈페이지로 보내달라는 안내**를
> 넣었습니다. 네이버 예약에는 파일 첨부가 없어서, 사진은 홈페이지 견적 폼이 받습니다.
>
> - **검수는 영업일 2~3일** 걸리고, 결과가 **`poing7003@naver.com`** 으로 옵니다.
> - **메일이 오면 알려주세요.** 통과했으면 제가 `예약받기 시작` + `플레이스 연결하기`
>   두 개만 누르면 플레이스에 버튼이 붙습니다. 반려됐으면 사유대로 고치겠습니다.
> - 검수 중에는 내용 수정이 제한될 수 있으니, 문구를 바꾸실 거면 결과 나온 뒤가 안전합니다.
>
> ⚠️ 예약 화면의 **문의 전화가 `042`** 로 나옵니다. 그 칸은 고칠 수 없고 **플레이스
> 기본정보를 `010` 으로 바꾸면 같이 바뀝니다** — 위 "검토가 끝나면" 항목과 같은 작업입니다.

대전 손님은 네이버에서 찾고, **지역 유입의 8할**이 여기서 옵니다.
플레이스가 비어 있으면(사진·리뷰·소개 없음) 전화는 잘 안 옵니다.

| | 어디서 | 걸리는 시간 |
|---|---|---|
| ~~네이버 스마트플레이스~~ ✅ 완료 | smartplace.naver.com | — |
| **네이버 서치어드바이저** — 사이트맵 + `rss.xml` 제출 | searchadvisor.naver.com | 색인 14~16일 |
| **구글 비즈니스 프로필** — 검색 우측 회사 카드가 여기서 생깁니다 | business.google.com | 인증 방식은 구글이 지정 |

> 신규 스마트플레이스는 **90일간 노출 우대**를 받습니다. 지금 등록하는 게 가장
> 효율이 좋습니다. 네이버 소유확인 코드를 주시면 `config/site.ts` 에 넣어 드립니다.

전체 절차와 등록 후 운영(블로그·리뷰·사진)은 [`docs/SEO.md`](docs/SEO.md) **C 섹션**에 있습니다.

#### 구글 쪽에 남은 것 두 가지

1. **색인 확대** — 지금 홈 1페이지만 들어가 있습니다. Search Console →
   **Sitemaps** 에 `sitemap.xml` 제출 + **URL 검사**로 `/works` `/about` 색인 요청.
2. **AI 가 홈페이지를 인용하게 하기** — 제미나이가 회사는 아는데 근거로 잡코리아·
   비즈노를 답니다. **차단 문제가 아니고**(크롤러 8종 정상 통과 확인), 인용할 사실이
   홈페이지에 부족한 것이 원인입니다. 인스타그램·사업자등록번호는 이미 물려 뒀고,
   **남은 건 시공사진과 시공사례 상세**입니다(위 1번).

> ⚠️ **구글 반영에는 시간이 걸립니다.** 검색 결과의 로고(파비콘)와 사이트 이름,
> AI 개요의 근거 자료는 재크롤링 후에 바뀝니다 — 구글 공식 문서 기준 며칠~몇 주.
> 오늘 고친 것이 내일 안 보이는 게 정상입니다.

#### 색인 통보 — `npm run indexnow`

```bash
npm run indexnow
```

Bing·네이버 등에 "이 주소들 읽어 가라"고 즉시 통보합니다(계정 불필요).
**페이지를 새로 만들거나 실적을 크게 늘린 뒤** 한 번 실행하세요.

> 구글은 이 방식에 참여하지 않습니다. 네이버는 위 서치어드바이저 등록이 끝나야
> 접수됩니다(등록 전에는 403).

### ✅ 방문 통계 켰습니다 (2026-08-06)

**누가 어디서 들어오는지** 이제 보입니다. Google Analytics 를 붙였습니다.

- 보는 곳: [analytics.google.com](https://analytics.google.com) → 속성 **`수산나디자인 홈페이지`**
- **데이터가 쌓이는 데 최대 48시간** 걸립니다. 바로 0으로 보여도 정상입니다.
- 네이버·구글·당근 중 **어디서 오는지**, 어느 페이지를 보고 나가는지가 잡힙니다.

> ⚠️ 애널리틱스에 비슷한 이름의 속성이 **3개** 있습니다. 홈페이지에 붙어 있는 건
> **`수산나디자인 홈페이지`** 하나뿐이고, `susanna-8bcad` 는 앱 프로젝트,
> `susanna1` 은 정체가 불분명합니다. 안 쓰는 건 지우는 게 좋습니다.

**개인정보처리방침도 같이 손봤습니다.** 방문 통계는 쿠키를 쓰기 때문에 고지가 법적
의무입니다. 어디에 무엇을 맡기고 있는지(구글·Resend·Cloudflare·Supabase)도 함께 적었습니다.

### ⚠️ Google Ads — 돈 나가지 않습니다

광고 만들기 화면까지 들어갔다가 멈춰 있는 캠페인이 있습니다.

```
계정 848-526-3219 · 캠페인 24115193992 · 결제수단 미등록
```

**결제수단을 등록해야 광고가 나갑니다.** 카드를 넣은 적이 없으니 **게재도 과금도
없습니다.** 그냥 두셔도 됩니다.

> ⚠️ 그 화면의 **`건너뛰기` 는 나가는 버튼이 아닙니다** — 예산·결제 단계로 더
> 들어갑니다. 광고를 안 하실 거면 창을 닫으시면 됩니다.
>
> 완전히 닫고 싶으시면 브라우저의 **광고 차단기를 끈 뒤**(안 끄면 Ads 화면이 아예
> 동작하지 않습니다) 설정에서 계정 취소를 하시면 됩니다. 말씀 주시면 도와드리겠습니다.

### 3. 남은 회사 정보 채우기 → `config/site.ts`

회사소개서에서 옮길 수 있는 값은 이미 채워 넣었습니다 (상호·대표이사·연락처·주소·설립연도·자본금·임직원·인증 6종).
남은 `// TODO` 는 아래뿐입니다.

| 항목 | 키 | 비고 |
|---|---|---|
| 옥외광고사업자 등록번호 | `outdoorAdNo` | 채우면 푸터·회사개요에 자동 노출 |
| 우편번호 | `zip` | 35374 로 추정 입력해 둠, 확인 필요 |
| 운영시간 | `hours` | 평일 09:00~18:00 으로 가정 |
| 누적 시공 건수 | `config/content.ts` 의 `stats` | 현재 `0,000건` |
| 카카오톡 채널 | `kakaoChannelUrl` | 비워두면 톡상담 버튼 미노출 |
| 지도 | `mapUrl` | 카카오맵/네이버지도 공유 URL |

**전화번호 2개를 나눠 씁니다.**
`phone`(010-7449-4600)이 대표번호로 헤더·푸터·CTA·모바일 하단바 전부에 나가고,
`officePhone`(042-541-0171)은 회사개요·오시는길·푸터 법정표기 줄에만 병기됩니다.
유선만 노출하고 싶으시면 `config/site.ts` 에서 둘을 맞바꾸면 됩니다.

### 4. 견적 문의 **알림 메일** — 왜 안 왔고, 무엇이 남았나

문의는 **Supabase `quotes` 테이블에 저장**되고 `/admin/quotes` 에서 보실 수 있습니다.
저장에 실패하면 손님에게 "접수됐다"고 거짓말하지 않고 전화를 안내합니다.
**문의가 유실된 적은 없습니다** — 알림만 안 갔습니다.

#### 안 온 이유 (2026-08-06 확인)

보내는 주소를 `noreply@susannadesign.co.kr` 로 적어 뒀는데, 그 도메인이 메일 발송
서비스(Resend)에 **아직 등록되지 않아 발송 요청이 거부**되고 있었습니다.
스팸함에 들어간 게 아니라 **아예 나가지 않았습니다.**

```
403  The susannadesign.co.kr domain is not verified
```

받는 주소도 회사 대표 메일(한메일)로 잡혀 있어서, 지메일로는 올 수가 없었습니다.

#### 지금 상태 — **도메인 인증 끝났습니다** (2026-08-06 11:14)

- ✅ **`susannadesign.co.kr` 이 Resend 에 인증됐습니다.** 이제 알림 메일이
  **`noreply@susannadesign.co.kr`** 이라는 회사 주소로 나갑니다. 시험용 주소를
  거치지 않으니 한메일·네이버메일 스팸 필터도 훨씬 잘 통과합니다.
- ✅ **받는 사람 3명** — `fivepeople201@gmail.com`, `poing7003@naver.com`,
  `sujin4003@hanmail.net`(회사 대표 메일). 인증 전에는 지메일 한 곳만 됐습니다.
- ✅ `npm run notify:test` 로 **세 주소로 실제 발송되는 것까지 확인**했습니다
  (지메일은 받은편지함 도착 확인, 스팸함 아님).
- ⚠️ **한메일·네이버메일은 첫 메일이 스팸함으로 갈 수 있습니다.** 도메인 인증을
  했어도 그 도메인에서 메일이 나간 이력이 아직 없어서 그렇습니다. 받으시면
  **"스팸 아님" 으로 옮기고 주소록에 추가**해 두세요. 몇 통 쌓이면 안정됩니다.
- **손님이 올린 사진이 알림 메일에 그대로 붙어서 옵니다** (2026-08-06 추가).
  현장에서 휴대폰으로 메일만 열어도 무슨 간판인지 보입니다.
- `/admin/quotes` 맨 위에 **알림이 켜졌는지·어디로 가는지**가 표시됩니다.

넣은 DNS 레코드는 Cloudflare 에 4개입니다 (DKIM · MX `send` · SPF · DMARC).
Resend 가 같이 권하는 **루트 MX(수신용)는 일부러 넣지 않았습니다** — 우리는 보내기만
하면 되고, 넣으면 나중에 회사 메일 계정을 붙일 때 충돌합니다.

#### 손님이 올린 사진(첨부파일)

**2026-08-06 이전에는 사진을 보관하지 않았습니다.** 파일 이름만 적고 사진 자체는
버렸습니다. 실제로 8월 3일 연구개발특구진흥재단 문의에서 현판 예시 사진 2장이
그렇게 사라졌습니다 — 그 건은 손님께 다시 요청하셔야 합니다.

지금은 이렇게 됩니다.

| 어디서 | 어떻게 |
|---|---|
| **알림 메일** | 파일이 메일에 첨부돼서 옵니다. 파일마다 “이 메일에 첨부 / 화면에서 내려받기” 가 적힙니다 |
| **관리 화면** | `/admin/quotes` → 문의 아래 “첨부” 에서 파일 이름을 누르면 내려받습니다 |

**받는 파일 형식** (2026-08-06 확대)

사진 · PDF · **한글(hwp)** · 오피스(엑셀·워드·PPT) · AI/PSD/EPS · **CAD(dwg·dxf)** · zip

관공서는 사양서를 한글 파일로 보내고 도면은 CAD 로 옵니다. 예전에는 이것들이 파일
선택창에서 아예 안 골라졌습니다. 실행파일(exe 등)은 일부러 안 받습니다.

**용량: 파일 하나당 50MB · 한 번에 5개까지 · 합계 50MB** (2026-08-06 확대, 이전 10MB)

AI·PSD·CAD 원본은 보통 수십 MB 라 예전 10MB 로는 못 받았습니다.
12MB 가 넘는 파일은 **메일에 안 붙고** 관리 화면에서 내려받게 됩니다 — 메일마다
파일별로 “이 메일에 첨부 / 화면에서 내려받기” 가 적히니 그대로 따라가시면 됩니다.

- 사진은 **관리자만** 볼 수 있는 곳에 보관됩니다. 주소를 알아도 로그인 없이는
  열리지 않습니다 (손님 매장 사진·도면이라 그렇게 했습니다).
- 문의를 **삭제하면 사진도 같이 지워집니다.** 개인정보 보유기간(상담 후 6개월) 때문입니다.
- 화면에 **“파일 없음”** 이라고 적힌 건 8월 6일 이전 문의입니다.

> ✅ **`0006_quote_files.sql` 실행 완료 (2026-08-06).** 사진이 실제로 보관되는 것과,
> 로그인 안 한 사람은 열 수 없는 것까지 확인했습니다. 문의를 지우면 사진도 같이
> 지워집니다. 나중에 다시 점검하고 싶으시면 `npm run quotes:check` 를 실행하세요.

#### ✅ 끝난 것 — 값 2개를 **Secret 으로** 재등록 (2026-08-06)

한 번은 값 3개가 배포 때 통째로 지워졌었습니다. 실수가 아니라 Cloudflare 의
동작입니다 — 배포 명령이 워커 설정을 덮어쓰는데, 이때 **`Plaintext` 로 넣은 값은
사라지고 `Secret` 만 살아남습니다.**

지금은 이렇게 나뉘어 있어 **다음 배포에도 안 지워집니다.**

| 값 | 어디에 |
|---|---|
| `RESEND_API_KEY` | Cloudflare **Secret** |
| `QUOTE_NOTIFY_EMAIL` | Cloudflare **Secret** |
| `QUOTE_MAIL_FROM` | 코드 (`wrangler.jsonc`) |

> ⚠️ **앞으로 값을 더 넣으실 때도 반드시 `Secret`.** `Plaintext` 는 다음 배포에 지워집니다.
> **배포한 뒤 `/admin/quotes` 맨 위 안내가 주황색 경고가 아닌지** 한 번 봐 주세요 —
> 주황이면 값이 또 날아간 것입니다. 그게 유일한 확인 방법입니다.

#### 🔴 그다음 — API 키 새로 발급

지난번 키가 `Plaintext` 로 들어가 있어서 대시보드를 볼 수 있는 사람에게 그대로
보였고, 작업 중 화면 캡처에도 찍혔습니다.

1. resend.com → **API Keys** 에서 **새 키 발급**, 기존 키 **삭제**
2. 위 `RESEND_API_KEY` Secret 을 새 키로 교체
3. 내 컴퓨터 `.env.local` 의 키도 새 값으로 바꾸고 `npm run notify:test`

#### ✅ 끝난 것 — 도메인 등록 (2026-08-06)

`susannadesign.co.kr` 을 Resend 에 등록하고 Cloudflare DNS 에 레코드 4개를 넣어
**Verified** 를 받았습니다. 덕분에 회사 도메인으로 발송되고(스팸 필터 통과율 상승),
받는 주소를 여러 개 쓸 수 있게 됐습니다.

> ⚠️ 네이버웍스·Gmail 계정을 직접 연결하는 방식(SMTP)은 쓸 수 없습니다.
> Cloudflare 서버가 그 방식을 지원하지 않아 메일 발송 서비스를 하나 거쳐야 합니다.
> Resend 는 **하루 100통까지 무료**라 비용은 들지 않습니다.

메일은 휴대폰에서 열면 고객 번호가 큰 버튼으로 나오고, **누르면 바로 전화가 걸립니다.**

#### 카카오워크·슬랙으로 받기 (이메일 대신, 또는 함께)

받을 채팅방의 웹훅 주소를 넣으면 됩니다. 둘 다 넣으면 양쪽으로 갑니다.

```
QUOTE_WEBHOOK_URL = https://hooks.slack.com/services/...
```

알림이 실패해도 접수는 정상 처리되니 문의를 잃지 않습니다.

> ⚠️ **켜시면 개인정보처리방침을 손봐야 합니다.** 고객 이름·연락처가 외부 서비스
> (슬랙·Resend)로 나가는 것이라 **개인정보 처리 위탁**에 해당합니다. 방침의 위탁
> 항목에 업체명과 위탁업무를 적어야 합니다(개인정보보호법 제26조). 말씀 주시면
> 문구를 넣어 드리겠습니다.

---

## 구조

```
app/
  page.tsx              홈 (히어로 + 간편폼 → 간판종류 → 강점 → 공정 → 실적 → 장비 → CTA)
  quote/                무료 견적 문의 (전체 폼)
  signs/                간판종류
  works/                시공실적 (업종 필터)
  process/              제작공정
  about/                회사소개 + 보유장비 + 회사개요
  support/              고객지원 (FAQ · 오시는길)
  privacy/ terms/ no-email-collect/   법정 고지
  api/quote/route.ts    견적 문의 수신 (서버 재검증 · 허니팟 · 레이트리밋)
  admin/                관리자 화면 ★ (로그인 · 첫화면사진 · 주요실적)
  admin/actions.ts      저장·삭제·순서변경 서버 액션 (권한 재확인 + 캐시 갱신)
  sitemap.ts robots.ts not-found.tsx

supabase/migrations/    DB 스키마·권한정책·초기데이터 SQL
public/_headers         정적 자산(사진·로고·스크립트) 캐시 규칙 — Cloudflare 용

components/
  admin/                관리 화면 전용 (업로드 · 폼 · 셸)
  Header / Footer / FloatingBar        공통 레이아웃
  SiteChrome                           /admin 에서 공개 헤더·푸터 숨김
  admin/SessionKeeper                  관리자 로그인 유지 (브라우저에서 토큰 갱신)
  HeroSlider                           히어로 슬라이더 (막대 인디케이터)
  QuickQuoteForm                       히어로 인라인 3필드 폼
  QuoteForm                            전체 견적 폼
  PrivacyConsent                       개인정보 동의 (고지 4요소 포함)
  Img / Placeholder                    사진 자동 교체
  WorksGrid / Field / Section

config/
  site.ts               회사 정보 ★
  content.ts            간판종류 · 공정 · 실적 · 장비 · 폼 선택지 (비상용 폴백)
  sections.ts           페이지 문구 구역 명세 — 관리 화면이 이걸 읽어 칸을 그림

lib/
  validate.ts           폼 검증 (클라이언트·서버 공용)
  images.ts             사진 존재 여부 확인 (로컬 파일 · 업로드 URL 모두 처리)
  cms.ts                DB 에서 콘텐츠 읽기 + 실패 시 content.ts 로 폴백 ★
  auth.ts               로그인·관리자 권한 확인
  supabase/             접속 정보 · 클라이언트 3종 · 테이블 타입
```

`config/content.ts` 는 이제 **초기값 겸 비상용 콘텐츠**입니다. Supabase 를 연결하면
실제 내용은 DB 에서 오고, DB 를 못 읽을 때만 이 파일이 쓰입니다.

---

## 설계 근거

국내 중견기업 6곳 + 간판업체 3곳의 실제 DOM을 조사해 만들었습니다. 근거는 [`reference/SPEC.md`](reference/SPEC.md), 캡처는 `reference/*.png` 에 있습니다.

- **GNB 6개 + 강조 CTA 1개** — 삼익THK `문의하기 ↗` 패턴
- **히어로 아래 간판종류 5분할 바** — 이건창호·아이에스동서의 사업분야 바
- **막대형 인디케이터** — 이건·시스메이트 (점 아님)
- **견적폼 필드** — 비스퀘어 구조 + 홍간판의 `설치 층수` (고소작업차 여부가 갈리는 견적 변수)
- **푸터 법정 표기** — 개인정보처리방침 볼드 강조, 이메일무단수집거부 (6곳 공통 규격)

## 레이아웃 폭

좌우 여백은 `app/globals.css` 의 **`.wrap`** 한 곳에서 관리합니다. 헤더·푸터·모든 섹션이 이 클래스를 씁니다.

```
max-width       1520px          콘텐츠가 최대로 넓어지는 한계
padding-inline  20 → 32 → 48 → 64px   (모바일 / 640 / 1024 / 1440 이상)
```

`max-width` 를 키우면 큰 모니터에서 더 넓게, `padding-inline` 을 키우면 화면 가장자리 여백이 넓어집니다.
읽기용으로 일부러 좁게 둔 곳은 예외입니다 — 업무프로세스 `max-w-6xl`, 개인정보처리방침·이용약관 `max-w-3xl`.

## 스크롤 연출

- **헤더**: 홈에서는 히어로 사진 위에 투명하게 얹혀 있다가(흰 로고·흰 메뉴), 40px 넘게 내리면
  흰 배경 + 청록 로고로 바뀝니다. `components/Header.tsx` 의 `clear` 상태.
  다른 페이지는 처음부터 흰 헤더입니다.
- **히어로 패럴랙스**: 스크롤하면 배경이 살짝 당겨지며 어두워지고, 카피가 위로 밀려 사라집니다.
  `HeroSlider` 의 `FADE_DISTANCE`(520px)로 속도 조절.
- **다음 섹션 진입**: 사업영역 바가 둥근 모서리로 히어로 위에 올라탑니다 (`-mt-8 rounded-t-[28px]`).
- **떠오르기**: 각 섹션이 뷰포트에 들어오면 살짝 올라오며 나타납니다 (`components/Reveal.tsx`).
  카드들은 `delay` 로 순차 등장. IntersectionObserver가 안 돌아도 3초 뒤 무조건 보이게 하는
  안전장치가 들어 있습니다.
- **휠 한 번에 다음 섹션으로**: 히어로 위에서 마우스를 조금만 굴려도 사업영역 섹션까지
  780ms(easeInOutCubic) 동안 부드럽게 실려 갑니다. `HeroSlider` 의 `glide()`.
  아래 조건에서만 개입하고 나머지는 브라우저 기본 스크롤 그대로 둡니다.

  | 개입함 | 개입 안 함 |
  |---|---|
  | 스크롤 80px 안 (`SNAP_MAX_SCROLL`) | 그 아래 전부 — 히어로 중간에서 읽는 사람은 안 건드림 |
  | 아래로 굴릴 때 | 위로 굴릴 때 |
  | 위로 굴린 지 700ms 지난 뒤 | 위로 굴린 직후 (`UP_GUARD_MS`) — 올라가다 멈춘 사람이 살짝 내려도 안 끌려감 |
  | 빈 영역 | 자체 스크롤이 있는 요소 위 (약관 박스 등) |
  | 마우스 휠 | 터치 스크롤 (모바일) |
  | | `Ctrl+휠` 확대/축소, 이동 직후 260ms 쿨다운 |

  이동 중 터치·키 입력이 들어오면 즉시 멈춥니다.
- **SCROLL 유도**: 히어로 하단 인디케이터. 휠과 **똑같은** `glide()` 를 호출해 착지 위치가
  일치합니다. 도착 지점은 헤더 높이를 실측해 계산하므로 헤더 크기가 바뀌어도 따라갑니다.

모두 `prefers-reduced-motion: reduce` 를 존중해 자동으로 꺼집니다(휠 스냅 포함).

## 폼 안전장치

- 클라이언트 검증 + **서버 재검증** (클라이언트 우회 시 400)
- 허니팟 봇 트랩 (캡차는 전환을 깎아서 미사용)
- IP 기준 레이트리밋 (10분 5회)
- 첨부파일 최대 5개 · 개당 10MB
- 개인정보 동의는 체크박스 + **수집항목·목적·보유기간·거부권 고지** (개인정보보호법 제15조)

## 접근성

스킵 링크, `aria-current`, `aria-invalid` + `role="alert"` 인라인 에러, 폼 label 연결, `prefers-reduced-motion` 대응.
